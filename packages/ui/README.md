# UI

This package contains shared React components for the InfocusApp application.

## Components

The components in this package are built with React and styled with Tailwind CSS. You can import the components in your code like this:

```typescript
import { Button } from "ui";

function MyComponent() {
  return <Button>Click me</Button>;
}
```
